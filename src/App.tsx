import React, { FC } from "react";
import { Route, Routes } from "react-router-dom";

const App: FC = () => {
  return (
    <div className="container">
      <Routes></Routes>
    </div>
  );
};

export default App;
